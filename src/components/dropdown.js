// import React, {useRef} from "react"

// import { useOutsiderAlert } from "../utils"

// export default props => {

//     const wrapperRef = useRef(null);
//     useOutsideAlerter(wrapperRef);

//     return <div ref={wrapperRef}>{props.children}</div>;

// }
